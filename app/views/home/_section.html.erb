<section class="gap-4 lg:px-80">
    <%= render partial: "section_header" , locals: {count: count, title: section.title } %>
    <% section.projects.each do |project| %>
        <%= render partial: "projects/project" , locals: {project: project} %>
    <% end %>
</section>
